# apps/web — variables públicas (NEXT_PUBLIC_*) van al bundle del cliente: NUNCA secretos.
# URL de apps/api (SSO + waitlist). En prod: https://auth.osia.com
NEXT_PUBLIC_API_URL=http://localhost:4000
# Deep-link de cruce de umbral por experiencia (dev). En prod cae al catálogo (*.osia.com).
NEXT_PUBLIC_WORLD_URL=http://localhost:3000
NEXT_PUBLIC_SOCIAL_URL=http://localhost:3002
