import { DiscoverView } from '../_components/DiscoverView';

/**
 * "/descubrir" (S3.11) — a quién seguir (sugeridos por reputación, sin IA/ML). El shell y la sesión los
 * provee `AppFrame`.
 */
export default function DiscoverPage() {
  return <DiscoverView />;
}
