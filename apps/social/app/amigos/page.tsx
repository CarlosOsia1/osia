import { FriendsView } from '../_components/FriendsView';

/**
 * "/amigos" (S3.9) — tu red: Seguidores · Seguidos · Solicitudes (aceptar/rechazar). El shell y la
 * sesión los provee `AppFrame`.
 */
export default function FriendsPage() {
  return <FriendsView />;
}
